{
  "name": "@sym/adapter-slack",
  "version": "0.0.0",
  "private": true,
  "description": "Sym Slack adapter — Block Kit rendering, event normalisation, Web API client wrappers.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "author": "Amit Ray",
  "repository": {
    "type": "git",
    "url": "https://github.com/amitray007/sym",
    "directory": "packages/adapter/slack"
  },
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc -p tsconfig.test.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@sym/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
