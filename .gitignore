# Dependencies
node_modules
.pnpm-store

# Build output
dist
build
out
.next
.turbo
*.tsbuildinfo

# Coverage
coverage
.nyc_output

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment — NEVER commit. Dokploy injects in prod.
.env
.env.*
!.env.example
!.env.*.example

# Secrets
*.pem
*.key
*.crt
*-private.pem
sym-encryption-key*

# Editor + OS
.DS_Store
Thumbs.db
*.swp
*.swo
.idea
*.iml
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Transient agent worktrees (parallel stream builds)
.claude/worktrees/

# Local credential store (operator control tier — intentional, not dead)
*.sqlite
*.sqlite3
*.db
*.db-journal

# Test artifacts
.vitest-cache
test-results

# Slack manifest — rendered output (generated from slack/manifest.template.yml)
slack/manifest.yml

# Misc
.cache
.parcel-cache
.eslintcache
tmp/

# Personal server provisioning scripts (custom /data setup, not part of the build)
dev/
